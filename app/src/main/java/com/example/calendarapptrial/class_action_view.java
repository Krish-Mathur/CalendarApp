package com.example.calendarapptrial;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class class_action_view extends AppCompatActivity { // Assuming it's an activity, I added "Activity" suffix for clarity
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.class_action_activity); // Assuming you have a layout file named "class_action_activity.xml"
    }
}
